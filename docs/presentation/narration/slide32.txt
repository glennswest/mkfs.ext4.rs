Ten days of commits, in five lines.
Version two point oh brought a no standard library read path, so firmware links this reader instead of growing a second implementation that drifts against it. Two point oh point two fixed the phantom journal. Two point oh point four replaced a release that shipped with an empty cargo manifest — a tag pushed with no build between the edit and the release. The broken tag stands, marked, rather than changing meaning under anyone who fetched it.
Two point one added the cache, and fio ext four one point five streams its unpacks through it.
[beat=0.5] Seven issues filed across the two repositories. Seven closed.
[e=0.85] Every one of them followed the deck's own script: measured on something real, fixed against the reference, and pinned by a test that fails without the fix.
